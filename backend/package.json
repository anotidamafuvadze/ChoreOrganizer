{
  "name": "term-project-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal backend for term-project using Node/Express",
  "main": "index.ts",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "ts-node -r dotenv/config index.ts",
    "dev": "nodemon --watch . --exec ts-node -r dotenv/config index.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/node": "^24.10.1",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
